body {
    position: relative;
    background-color: #FFF;
    .wrapper-braslet {
        .content-wrapper {
            clear: both;
            // overflow: hidden;
            position: relative;
        }
    }
}

*, ::after, ::before {
    box-sizing: border-box;
}
